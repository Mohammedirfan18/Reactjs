import Chai from "./Chai"

function App() {


  return (
      
      <>
      <pre>
      <p>Chai aur Reactjs |React is a js library we can use 
        react to create complex ui | React can be install using npx create-react-app but it is heavy.
        so we can use vite which will download only required dependencies|
        the commands to run the project are mention in package.json file|
         Mohammed Irfan </p>
      </pre>
      <Chai/>
      </>
  )
}

export default App
